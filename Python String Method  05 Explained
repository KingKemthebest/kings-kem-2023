name = "Bro"

print(len(name)) #technically this is a function
# returns the length of an object. For example, it can return the number of items in a list.

print(name.find("o"))
# find the index of the first occurrence of a substring from the given string

print(name.capitalize())
# converts the first characters of a string to an uppercase letter and all other alphabets to lowercase

print(name.upper())
# Changes all character to uppercase

print(name.lower())
# changes all characters to lowercase

print(name.isdigit())
# returns True if all the characters are digits, otherwise False

print(name.isalpha())
# returns True if all the characters are alphabet letters (a-z).

print(name.count("o"))
# It returns the number of times a given value occurs in a string or a list

print(name.replace("o","a"))
# The replace() method replaces a specified phrase with another specified phrase

print(name*3)
